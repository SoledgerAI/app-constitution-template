# Domain Templates

This folder contains reusable templates for defining the domain package of a new application.

Each real application must complete these templates before implementation begins.

Domain templates define the app's truth, entities, invariants, rules, lifecycle states, events, and observability requirements.

These templates are not examples. Worked examples belong under the `examples/` folder.
