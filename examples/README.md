# Examples

This folder contains worked example domain packages that show how completed app specifications should look.

Examples are for reference only.

They must not be treated as reusable templates.

Each example should live in its own application-specific folder.

The reconciliation ledger example belongs under:

```txt
examples/reconciliation-ledger/
```
